#!/bin/bash
#
# Arguments:
#
#   $1  - Number of MPI Tasks
#   $2+ - Executable and its arguments
#

PARTITION=compute
#QOS=debug

NP=$1
shift

# srun -A $ACCOUNT -q $QOS -t 0:01 -n $NP $@
srun --mpi=pmix --partition=$PARTITION -t 0:01 -n $NP $@
